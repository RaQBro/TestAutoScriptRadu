VIEW "sap.plc.extensibility::template_application.v_messages"
AS
SELECT 
	TIMESTAMP,
	JOB_ID,
	SEVERITY,
	TEXT,
	DETAILS,
	OPERATION,
	MESSAGE_ID,
	PLC_OBJECT_TYPE,
	PLC_OBJECT_ID
FROM "sap.plc.extensibility::template_application.t_messages"